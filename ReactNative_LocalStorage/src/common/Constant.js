/**
 * Chứa những biến mà dev muốn Hard Code
 * (Từ giá trị Hard Code thì muốn biến nó thành biến)
 */

// export const KEY_TOKEN = 'token'

//Giá trị KEY_STORAGE là kiểu Object:
export const KEY_STORAGE = {
    token: 'token',
    category: 'category',
}

export const KEY_SCREENS ={
    login:'login',
    homepage:'homepage',
}